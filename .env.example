# Transport mode: "stdio" or "http" (default: http)
MCP_TRANSPORT=http

# HTTP server bind address (http mode only)
HOST=127.0.0.1
PORT=3000

# Logging level: trace | debug | info | warn | error
LOG_LEVEL=info
